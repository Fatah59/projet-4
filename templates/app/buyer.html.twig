{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur la Billeterie !{% endblock %}

{% block body %}

    <section class="section">

        <div class="container">

            <h2>Coordonnées de l'acheteur :</h2>

            <div class="form-group row">

                {{ form_start(buyerForm) }}
                <div class="col-sm-12">
                    {{ form_row(buyerForm.buyer.lastName) }}
                </div>


                {{ form_row(buyerForm.buyer.name) }}
                {{ form_row(buyerForm.buyer.email) }}

                <h2>Adresse de facturation :</h2>
                {{ form_row(buyerForm.buyer.adress) }}
                {{ form_row(buyerForm.buyer.zipcode) }}
                {{ form_row(buyerForm.buyer.city) }}
            </div>

            <div class="row">

                <button type="submit" class="btn btn-primary">Valider</button>
            </div>



            {{ form_end(buyerForm) }}

        </div>

    </section>

{% endblock %}